<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appointment_management">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_img"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_img"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SetAppointments" />
        <activity android:name=".EditAppointment" />
        <activity android:name=".DeleteAppointments" />
        <activity android:name=".MoveAppointments" />
        <activity android:name=".SearchAppointment"></activity>
    </application>

</manifest>